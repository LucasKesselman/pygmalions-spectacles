
Tasks

    ✔️ - Get Sphere to display in AR anchored to trained image via Zappar
        ✔️ - train image
        ✔️ - display Sphere in AR
        ✔️ - Lock sphere to target
        ✔️ - ensure /public target is being bundled in webpack/hosting process (tsc,vite,firebase)


    Display user info in the landing modal
        userid, username, paswordhash, password, designs


    Dynamically load different Designs into the AR world
        for now, add a text box where we can input a design ID ("D-001") -> check if user has access to that design -> output an AR scene with that design ("D-001-target.zpt","D-001-Scene : ArtieScene")


    Implement a web navigation around the AR app part
        HTML pages:  
            Home Page. User Login Screen. My Designs page. AR App page (with canvas div).
